# 策略设计 — 交易全流程详解（2026-05-09 更新）

> 涉及文件：
> - `backend/stock/scheduler/tasks_daily_open.py` —— 开盘执行（开仓/平仓/移仓/加仓）
> - `backend/stock/scheduler/tasks_daily_close.py` —— 收盘计算（信号生成/止损更新）
> - `backend/stock/scheduler/tasks_exit_before_close.py` —— 临收盘紧急止损
> - `backend/stock/core/position_sizing.py` —— Unit 手数计算
> - `backend/stock/infrastructure/order_execution.py` —— 最小开仓限制与两步开仓
> - `backend/stock/core/atr.py` —— ATR 计算与跳空保护
> - `backend/stock/core/config_loader.py` —— 全局参数

---

## 一、开仓逻辑（ENTRY）

### 1.1 触发条件

```python
def check_breakout_signal(klines, entry_period=20):
    # 取前 entry_period 根 K 线的高低点（不含最新一根）
    high_prior = klines["high"].iloc[-entry_period - 1:-1]
    low_prior = klines["low"].iloc[-entry_period - 1:-1]
    
    entry_high = float(high_prior.max())
    entry_low = float(low_prior.min())
    latest_close = float(klines.iloc[-1]['close'])
    
    if latest_close > entry_high:
        → 向上突破，signal_direction = 1（做多）
    elif latest_close < entry_low:
        → 向下突破，signal_direction = -1（做空）
```

**关键规则**：
- 突破判断使用**前 20 日的高低点，不包含当日 K 线**
- 收盘后计算，次日开盘执行
- 信号生成时检查：
  1. 该品种当前 `units == 0`（无持仓）
  2. 不存在未执行的 `ENTRY` 信号（`executed_status='PENDING'`）
  3. 该品种在当前账户的 `AccountContractConfig` 中已激活（`is_active=True`）

### 1.2 仓位计算

```python
def calculate_unit_lots(api, symbol):
    contract = api.get_quote(symbol)
    volume_multiple = contract.volume_multiple  # 合约乘数
    
    # 计算 20 日 ATR
    klines = api.get_kline_serial(symbol, duration_seconds=86400, data_length=25)
    # ... 计算 TR 列表，取最近 20 个 TR 的平均值 → atr_20
    
    unit_lots = POSITION_RISK_BASE_AMOUNT / (atr_20 * POSITION_RISK_MULTIPLIER * volume_multiple)
    unit_lots = round(unit_lots)
    unit_lots = max(1, unit_lots)
    return unit_lots
```

**公式**：`unit_lots = 4000 / (ATR × 2 × 合约乘数)`

**示例**：螺纹钢 ATR=50，乘数=10 → `unit_lots = 4000 / (50×2×10) = 4 手/Unit`

### 1.3 跳空保护

```python
def price_gap_protection(api, symbol, direction, gap_threshold_atr_multiplier=1.5):
    atr = calculate_atr(api, symbol)
    quote = api.get_quote(symbol)
    gap_ratio = abs(quote.last_price - quote.pre_close) / atr
    
    if gap_ratio > gap_threshold_atr_multiplier:
        → 禁止开仓（信号状态设为 CANCELLED，备注"跳空保护"）
```

> 跳空幅度 = |最新价 - 昨收价| / ATR，超过 1.5 倍 ATR 时跳过开仓，防止追高/追低。

### 1.4 最小开仓手数限制（两步开仓）

当 `planned_volume < min_position`（交易所最小开仓手数）时：

```
第1步：开立 min_position 手（满足交易所要求）
第2步：立即平仓 (min_position - planned_volume) 手
结果：最终持仓 = planned_volume 手，合规且达到目标
```

> 例如：计划开 2 手，但交易所最小开仓 5 手 → 先开 5 手，再平 3 手，最终持仓 2 手。

### 1.5 开仓执行流程

```python
def execute_entry_order(api, account, signal):
    1. 检查交易时间 is_trading()
    2. 计算 unit_lots → order_volume = 1 * unit_lots
    3. 跳空保护检查
    4. 最小开仓手数检查
    5. TargetPosTask 设置目标持仓
       - 多头：target_lots = +order_volume
       - 空头：target_lots = -order_volume
    6. wait_for_target_position(timeout=60s)
    7. 成交后更新 PositionState：
       - units = 1
       - direction = signal_direction
       - contract_total_position = actual_filled
       - last_add_price = entry_avg_price
       - first_open_price = entry_avg_price
       - highest_close = entry_avg_price
       - lowest_close = entry_avg_price
       - open_date = today
    8. 信号状态更新为 SUCCESS
```

---

## 二、平仓逻辑（STOP_LOSS / EXIT）

### 2.1 收盘后止损信号生成

```python
def check_exit_signals():
    for position in 所有有持仓:
        if 多头 and latest_close_price < stop_loss_price:
            → 生成 STOP_LOSS 信号
        if 空头 and latest_close_price > stop_loss_price:
            → 生成 STOP_LOSS 信号
        
        # 防重复：检查是否已存在未执行的 STOP_LOSS 信号
        if 存在 PENDING 的 STOP_LOSS:
            → 跳过
```

### 2.2 临收盘紧急止损（14:57）

```python
def check_and_execute_stop_loss(api):
    # 数据库层面直接过滤触发止损的持仓
    positions = PositionState.objects.filter(
        Q(direction=1, latest_close_price__lt=F('stop_loss_price')) |
        Q(direction=-1, latest_close_price__gt=F('stop_loss_price'))
    )
    
    for position in positions:
        1. 检查是否存在未执行的 STOP_LOSS 信号（防重复）
        2. 创建 STOP_LOSS 信号（executed_status='EXECUTING'）
        3. TargetPosTask 设置目标持仓 = 0（市价平仓）
        4. 等待成交（timeout=60s）
        5. 创建 ClosedPositionRecord
        6. 清空 PositionState（units=0, direction=0, stop_loss_price=None...）
        7. 更新信号状态为 SUCCESS
```

> **14:57 与开盘止损的区别**：14:57 是紧急风控，发现即平；开盘止损是按信号队列顺序执行。

### 2.3 开盘止损执行流程

```python
def process_signals_by_type(api, account, 'STOP_LOSS'):
    # 遍历所有有持仓的品种
    positions = PositionState.objects.filter(account=account, units__gt=0)
    for position in positions:
        exit_signals = DailyStrategySignal.objects.filter(
            symbol=position.symbol, trade_type='STOP_LOSS', executed_status='PENDING'
        )
        for signal in exit_signals:
            execute_exit_order(api, position, signal)
```

```python
def execute_exit_order(api, position, signal):
    1. 检查交易时间 is_trading()
    2. TargetPosTask 设置目标持仓 = 0
    3. wait_for_target_position(timeout=60s)
    4. 获取平仓价（quote.last_price）
    5. 计算盈亏 pnl = (exit_price - cost_price) * volume * volume_multiple
    6. 创建 ClosedPositionRecord
    7. 清空 PositionState
    8. 信号状态更新为 SUCCESS
```

---

## 三、移仓逻辑（ROLLOVER）

### 3.1 触发条件

```python
def check_rollover_signals(account):
    # sync_contract_list_from_tqsdk() 已标记 is_rollover_needed=True 的持仓
    positions = PositionState.objects.filter(
        account=account, is_rollover_needed=True, units__gt=0
    )
    
    for position in positions:
        main_contract = FullContractList.objects.filter(
            product_code=position.product_code
        ).first()
        
        # 生成 ROLLOVER 信号（平旧合约 + 开新合约）
        DailyStrategySignal.objects.create(
            trade_type='ROLLOVER',
            symbol=main_contract.symbol,  # 新合约
            remark=f"需要移仓到新主力合约 {main_contract.symbol}"
        )
```

### 3.2 移仓执行流程

```python
def execute_rollover_order(api, position, signal):
    # ========== 第1阶段：平仓旧合约 ==========
    target_pos_old = TargetPosTask(api, position.symbol)
    target_pos_old.set_target_volume(0)
    wait_for_target_position(...)
    
    # ========== 第2阶段：开仓新合约 ==========
    target_volume = position.contract_total_position  # 同方向同手数
    
    # 从 FullContractList 获取新主力合约代码
    main_contract = FullContractList.objects.filter(
        product_code=position.product_code
    ).first()
    new_symbol = main_contract.symbol
    
    # 检查最小开仓限制
    min_position_check = check_min_position_requirement(new_symbol, target_volume)
    if min_position_check['need_adjustment']:
        → 两步开仓 (使用 new_symbol)
    else:
        target_pos_new = TargetPosTask(api, new_symbol)
        target_pos_new.set_target_volume(target_lots)
        wait_for_target_position(...)
    
    # ========== 第3阶段：更新数据库 ==========
    # 基于新合约 20 日历史数据初始化 highest/lowest/止损价
    klines = api.get_kline_serial(new_symbol, duration_seconds=86400, data_length=25)
    historical_high = klines['close'].rolling(window=20).max().iloc[-1]
    historical_low = klines['close'].rolling(window=20).min().iloc[-1]
    atr_value = calculate_atr(api, new_symbol)
    
    # 创建新 PositionState（使用 new_symbol）
    PositionState.objects.update_or_create(
        account=position.account, symbol=new_symbol,
        defaults={
            'direction': position.direction,
            'units': position.units,
            'highest_close': historical_high (多头) / None (空头),
            'lowest_close': historical_low (空头) / None (多头),
            'stop_loss_price': 根据方向计算,
            'is_rollover_needed': False
        }
    )
    # 删除旧合约 PositionState
    PositionState.objects.filter(id=position.id).delete()
```

> **优先级**：开盘任务中，ROLLOVER 在 ENTRY 之后、ADD_ON 之前执行。

---

## 四、加仓逻辑（ADD_ON）

### 4.1 加仓信号生成（收盘后）

```python
def check_add_position_signals():
    positions = PositionState.objects.filter(units__gt=0, units__lt=3)
    
    for position in positions:
        atr_value = position.indicators['atr_20']
        latest_price = position.latest_close_price
        
        if current_units == 1:
            # 基准价 = last_add_price
            price_diff = latest_price - last_add_price
            
            if 多头:
                if price_diff > 1.0 * atr_value: → add_units = 2（直接满仓）
                elif price_diff > 0.5 * atr_value: → add_units = 1
            if 空头:
                if price_diff < -1.0 * atr_value: → add_units = 2
                elif price_diff < -0.5 * atr_value: → add_units = 1
        
        elif current_units == 2:
            # 基准价 = first_open_price
            if 多头:
                price_diff = latest_price - first_open_price
                if price_diff > 1.0 * atr_value: → add_units = 1
            if 空头:
                price_diff = first_open_price - latest_price
                if price_diff > 1.0 * atr_value: → add_units = 1
        
        # 安全截断
        if current_units + add_units > 3:
            add_units = 3 - current_units
        
        # 防重复
        if 存在 PENDING 的 ADD_ON 信号: → 跳过
        
        DailyStrategySignal.objects.create(
            trade_type='ADD_ON',
            contract_target_number=add_units,
            ...
        )
```

### 4.2 加仓执行流程

```python
def execute_add_on_order(api, account, signal):
    1. 检查交易时间 is_trading()
    2. 从 signal.contract_target_number 获取加仓单位数
    3. 检查 projected_units = position.units + add_units > 3 ?
       → 是：取消信号（CANCELLED）
    4. 计算 order_volume = add_units * unit_lots
    5. 检查最小开仓限制
    6. TargetPosTask 设置目标持仓
       - 多头：target_lots = current_position + order_volume
       - 空头：target_lots = -(current_position + order_volume)
    7. wait_for_target_position(timeout=60s)
    8. 更新 PositionState：
       - units += add_units
       - contract_total_position += order_volume
       - last_add_price = avg_price
    9. 信号状态更新为 SUCCESS
```

### 4.3 加仓价格坐标推导（多头示例）

假设开仓价 P₀=5000，ATR=40：

```
价格轴（向上为正）:
                         价格
                          │
         第3加仓点 ──────┼────── 5040  ← 1Unit时涨超1.0A，+2Unit（直接满仓）
                          │              2Unit时累计涨超1.0A，+1Unit（满仓）
         第2加仓点 ──────┼────── 5020  ← 1Unit时涨超0.5A，+1Unit
                          │
         开仓点   ──────┼────── 5000  ← ENTRY（唐奇安突破）
                          │
                          ▼
```

**关键观察**：
- 价格从 5000 → 5020：+1Unit（总 Unit=2），基准切换为 first_open_price=5000
- 价格从 5000 → 5040：+2Unit（总 Unit=3，直接满仓）
- 若先涨到 5020（2Unit），再涨到 5040：累计涨幅 = 40 = 1.0×ATR → 再触发 +1Unit 到满仓

---

## 五、止损逻辑（STOP LOSS）

### 5.1 动态跟踪止损

```python
def update_all_positions_stop_loss_price(api):
    for position in 所有有持仓:
        atr_value = position.indicators['atr_20']
        factor = position.indicators['trend_factor']
        
        if 多头:
            dynamic_stop_loss = highest_close - 2 * (1 + factor) * atr_value
        if 空头:
            dynamic_stop_loss = lowest_close + 2 * (1 + factor) * atr_value
```

| factor | 止损倍数 | 特性 |
|--------|---------|------|
| 0.0 | 2.0 ATR | 震荡市，止损收紧 |
| 0.25 | 2.5 ATR | 中等趋势 |
| 0.5 | 3.0 ATR | 强趋势，给足回调空间 |

### 5.2 保本兜底机制

```python
# 保本启用条件（单向开关，只开不关）
盈利 = |最新收盘价 - 成本价|
if 盈利 > 2.5 × ATR:
    protect_cost_enabled = True

# 保本价（按品种 tick size 计算）
tick = FullContractList.objects.get(symbol=position.symbol).price_tick
多头保本价 = 成本价 + tick × 2
空头保本价 = 成本价 - tick × 2

# 最终止损（保本只是底线）
多头：max(动态止损, 保本价)
空头：min(动态止损, 保本价)
```

### 5.3 止损触发检查（双保险）

| 检查时机 | 触发条件 | 执行方式 |
|---------|---------|---------|
| **15:02 收盘后** | 收盘价击穿止损价 | 生成 STOP_LOSS 信号，次日开盘执行 |
| **14:57 临收盘** | 最新价击穿止损价 | 立即市价平仓（紧急风控） |
| **09:02/21:02 开盘** | 执行 PENDING 的 STOP_LOSS 信号 | TargetPosTask 平仓 |

---

## 六、信号状态流转

```
PENDING（待执行）
    │
    ├── 开盘任务 pickup ──► EXECUTING（执行中）
    │                           │
    │                           ├── 成交成功 ──► SUCCESS
    │                           │
    │                           ├── 成交失败 ──► FAILED
    │                           │
    │                           └── 条件不满足（跳空/超限）──► CANCELLED
    │
    └── 收盘前未执行 ──► 次日继续 PENDING（跨日保留）
```

**防重复机制**：同一品种、同一 trade_type、同一交易日只生成一个 PENDING 信号。若前一日信号未执行，次日不会重复生成。

---

## 七、执行优先级（开盘任务）

```python
def job_daily_open_process():
    # 按优先级处理信号
    1. STOP_LOSS  — 最高（止损平仓，控制风险）
    2. ENTRY      — 次高（趋势开仓，捕捉机会）
    3. ROLLOVER   — 中等（移仓换月，保持持仓）
    4. ADD_ON     — 最低（金字塔加仓，扩大盈利）
```

> **为什么止损最高？** 防止止损和加仓/开仓冲突。若某品种同时触发止损和加仓，先平后开，避免持仓方向混乱。

---

## 八、关键参数汇总

| 参数 | 值 | 作用 |
|------|------|------|
| `POSITION_RISK_BASE_AMOUNT` | 4000 | 每 Unit 风险基数 |
| `POSITION_RISK_MULTIPLIER` | 2 | 止损距离 = 2×ATR |
| `POSITION_MAX_UNITS` | 3 | 最大持仓 Unit 数 |
| `PROTECT_COST_ENABLED_RATIO` | 2.5 | 盈利超过 2.5×ATR 启用保本 |
| `GAP_PROTECTION_RATIO` | 1.5 | 跳空 > 1.5×ATR 跳过开仓 |
| `TIMEOUT_SECONDS` | 60 | 订单超时 |
| `TREND_GAP_LIMIT` | 0.03 | 趋势因子封顶上限 |
| `TREND_FACTOR_MAX` | 0.5 | 趋势因子最大值 |
