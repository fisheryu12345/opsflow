"""Signal handlers — (deprecated, unified RBAC replaces dvadmin role sync).

Previously synced IAM membership roles → dvadmin Roles.
The unified IAMPermission model now handles all permission checks directly,
so this bridge is no longer needed.
"""
