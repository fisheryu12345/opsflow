{
  "name": "netapp_create_snapshot",
  "description": "为 NetApp 存储卷创建快照（注意：这是存储设备操作，不能用于 ESXi 虚拟机快照）",
  "risk_level": "low",
  "group": "storage",
  "component_code": "opsflow_netapp_create_snapshot",
  "executor_type": "netapp",
  "inputs": [
    {"name": "cluster_ip", "type": "string", "required": true},
    {"name": "volume_uuid", "type": "string", "required": true},
    {"name": "snapshot_name", "type": "string", "required": true}
  ],
  "outputs": [{"name": "snapshot_name", "type": "string"}],
  "rollback": null,
  "dependencies": []
}
