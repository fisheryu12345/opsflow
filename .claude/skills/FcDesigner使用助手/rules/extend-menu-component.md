# 扩展组件与菜单（DragRule）

## 要点
- **DragRule**：`name` 唯一；`rule()` 生成默认规则；`props()` 定义右侧面板；可扩展 `event`、`validate`、`watch`、`parseRule` / `loadRule`。
- **菜单**：`addMenu` 增加分组；`addComponent` 将组件挂到目标 `menu`。

## 关联
- 右侧面板字段扩展：`panel-base-component-rule.md`
- 扩展示例片段：`adv-code-snippets.md`（9.2、9.20 等）
